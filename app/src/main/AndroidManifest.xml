<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs490.team_15.vibe">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".DJLoginActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".TabActivity" />
        <activity
            android:name=".ExampleActivity"
            android:label="@string/title_activity_example"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DemoTabLayout"
            android:label="@string/title_activity_demo_tab_layout"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DJTabActivity"
            android:label="@string/title_activity_djtab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DJTabbedActivity"
            android:label="@string/title_activity_djtabbed"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>